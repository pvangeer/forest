<Window x:Class="StoryTree.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:modelViews="clr-namespace:StoryTree.Gui.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <modelViews:ProjectViewModel></modelViews:ProjectViewModel>
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate x:Key="EndEventTemplate" DataType="modelViews:TreeEventViewModel">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="30"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                    <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                    <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                </Grid.RowDefinitions>
                <Border Grid.Row="1" Grid.Column="0" CornerRadius="5" BorderThickness="2" BorderBrush="SaddleBrown">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Duration="0:0:0.400" To="3" Storyboard.TargetProperty="BorderThickness" />
                                                <ColorAnimation Duration="0:0:0.400" From="SaddleBrown" To="Green" Storyboard.TargetProperty="BorderBrush"></ColorAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Duration="0:0:0.250" To="2" Storyboard.TargetProperty="BorderThickness" />
                                                <ColorAnimation Duration="0:0:0.400" From="Green" To="SaddleBrown" Storyboard.TargetProperty="BorderBrush"></ColorAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
                <Grid Grid.Row="1" Grid.Column="0" Width="120">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"></RowDefinition>
                        <RowDefinition Height="25"></RowDefinition>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding Name}"></TextBlock>
                    <TextBlock Grid.Row="1" Text="{Binding Description}"></TextBlock>
                </Grid>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Falen" Background="MediumAquamarine" Height="50"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="ConnectingEventTemplate" DataType="modelViews:TreeEventViewModel">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="30"></ColumnDefinition>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                    <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                    <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                </Grid.RowDefinitions>
                <Border Grid.Row="1" 
                        Grid.Column="0" 
                        CornerRadius="1" 
                        BorderThickness="2" 
                        BorderBrush="SaddleBrown"
                        Background="AliceBlue">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Duration="0:0:0.400" To="3" Storyboard.TargetProperty="BorderThickness" />
                                                <ColorAnimation Duration="0:0:0.400" From="SaddleBrown" To="Green" Storyboard.TargetProperty="BorderBrush"></ColorAnimation>
                                                <ColorAnimation Duration="0:0:0.400" From="AliceBlue" To="DarkGray" Storyboard.TargetProperty="Background"></ColorAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <ThicknessAnimation Duration="0:0:0.250" To="2" Storyboard.TargetProperty="BorderThickness" />
                                                <ColorAnimation Duration="0:0:0.400" From="Green" To="SaddleBrown" Storyboard.TargetProperty="BorderBrush"></ColorAnimation>
                                                <ColorAnimation Duration="0:0:0.400" From="DarkGray" To="AliceBlue" Storyboard.TargetProperty="Background"></ColorAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
                <Grid Grid.Row="1" Grid.Column="0" Width="120">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding Name}"></TextBlock>
                    <TextBlock Grid.Row="1" Text="{Binding Description}"></TextBlock>
                </Grid>
                <ContentControl Grid.Row="0" Grid.Column="2" DataContext="{Binding TrueEvent}" Content="{Binding Self}" ContentTemplate="{DynamicResource TemplateSwitch}"/>
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="TemplateSwitch" DataType="modelViews:TreeEventViewModel">
            <ContentControl>
                <ContentControl.Style>
                    <Style TargetType="{x:Type ContentControl}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsConnectingEvent}" Value="True">
                                <Setter Property="ContentTemplate" Value="{DynamicResource ConnectingEventTemplate}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsConnectingEvent}" Value="False">
                                <Setter Property="ContentTemplate" Value="{DynamicResource EndEventTemplate}" />
                            </DataTrigger>
                        </Style.Triggers>
                        <Setter Property="Content" Value="{Binding Self}"></Setter>
                        <Setter Property="DataContext" Value="{Binding Self}"></Setter>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </DataTemplate>
    </Window.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="1" DataContext="{Binding MainTreeEventViewModel}" Content="{Binding Self}" ContentTemplate="{DynamicResource TemplateSwitch}"/>
    </Grid>
</Window>

